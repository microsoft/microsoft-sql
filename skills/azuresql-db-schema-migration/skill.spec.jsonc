{
  "$schema": "../../catalog/skill.spec.schema.json",
  "id": "azuresql-db-schema-migration",
  "domain": "azure-sql-database-container",
  "value": [
    "not-in-training-data"
  ],
  "correction": "Migration tools get pointed at the server default, which is master, and the run appears to succeed while putting the schema in the wrong place. The user database must be provisioned first and named in the connection string.",
  "posture": [
    "read",
    "write",
    "execute"
  ],
  "maturity": "preview",
  "applies_to": [
    "azure-sql-db-container"
  ],
  "validation": {
    "target": "container",
    "assert": [
      "user database provisioned before migrating",
      "connection string targets the user database"
    ]
  },
  "triggering": {
    "explicit": [
      "Use the azuresql-db-schema-migration skill"
    ],
    "implicit": [
      "run my Prisma migrations against the local database"
    ],
    "negative": [
      "set up a local PostgreSQL database with docker compose",
      "provision an Azure SQL Database in my subscription",
      "connect to SQL Server on my company network"
    ]
  }
}
