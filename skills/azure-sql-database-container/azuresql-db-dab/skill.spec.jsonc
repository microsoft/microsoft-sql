{
  "$schema": "../../../catalog/skill.spec.schema.json",
  "id": "azuresql-db-dab",
  "domain": "azure-sql-database-container",
  "value": [
    "not-in-training-data"
  ],
  "correction": "Asked for an API over existing tables, an agent hand-writes controllers and an ORM layer. Data API Builder generates REST and GraphQL from configuration with no application code, which is both less work and less to maintain.",
  "posture": [
    "read",
    "write",
    "execute"
  ],
  "maturity": "preview",
  "applies_to": [
    "azure-sql-db-container"
  ],
  "last_verified": "2026-08-20",
  "validation": {
    "target": "container",
    "assert": [
      "uses Data API Builder configuration rather than hand-written endpoints"
    ]
  },
  "triggering": {
    "explicit": [
      "Use the azuresql-db-dab skill"
    ],
    "implicit": [
      "give me a REST and GraphQL API over these tables without writing code"
    ],
    "negative": [
      "set up a local PostgreSQL database with docker compose",
      "provision an Azure SQL Database in my subscription",
      "connect to SQL Server on my company network"
    ]
  }
}
