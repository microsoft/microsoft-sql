{
  "$schema": "../../catalog/skill.spec.schema.json",
  "id": "github-actions-for-sql",
  "domain": "schema-change-and-cicd",
  "value": ["not-in-training-data", "corrects-a-wrong-assumption", "changes-too-fast-to-memorize", "multi-step-with-silent-failures"],
  "correction": "Wiring a workflow to deploy schema to Azure SQL Database, an agent composes the federated credential subject from the documented template `repo:OWNER/REPO:ref:refs/heads/main`, adjusting the job segment for the environment it just added. No amount of care with that segment helps, because the subject the platform actually presents is no longer the documented shape at all: the default prefix now carries numeric owner and repository ids, measured as `repo:OWNER@<ownerId>/REPO@<repoId>:environment:<name>` on a repository whose subject claim is reported as the default and not customised, which no hand-written credential will match. The prefix has to be read from the repository, not written. The identity provider rejects it with AADSTS700213, which does quote the presented subject verbatim, and the login step then prints `Login failed with Error: The process '/usr/bin/az' failed with exit code 1. Double check if the 'auth-type' is correct`, naming an input that is not the problem. The cost compounds one step later, because the deployment action opens the runner's firewall by parsing the client address out of a deliberately failed connection, so any authentication failure surfaces as `Failed to add firewall rule. Unable to detect client IP Address`, and a pipeline reports a firewall problem for an identity problem. The gate is also weaker than it reads: a required-reviewer rule is refused outright on a private repository whose plan does not include it, with the environment still created and the workflow still valid, and it only ever holds jobs that name the environment, so a second job in the same workflow reads the same repository secrets and deploys without waiting.",
  "value_probe": {
    "gate": "The subject registered for the credential is read from what the repository actually presents rather than composed by hand from a documented template, and the firewall message is identified as a consequence of the failed sign in rather than a second problem to fix.",
    "task": "Our workflow deploys schema changes to Azure SQL Database and it worked until I added a deployment environment to the job. Now the sign in step fails and the log tells me to double check that the auth type is correct, and the step after it says it could not detect the client IP address so it could not add a firewall rule. I have re-created the credential in Azure twice, matching the branch and the environment name exactly as the documentation shows. What is wrong, and how can I verify the fix before I push again?",
    "states": [
      "The subject the platform presents can carry numeric owner and repository ids, so it has to be read rather than written.",
      "The identity provider's own rejection quotes the subject that was presented, which is where to read the real value.",
      "An environment rule only holds jobs that name that environment, so another job in the same workflow deploys without waiting."
    ],
    "refutes": [
      "The subject to register is the documented template carrying the owner and repository names.",
      "The message about not detecting the client IP address means the firewall or the runner's networking is the problem.",
      "The auth type input named in the sign in failure is the input that needs correcting.",
      "A required reviewer rule on the environment can be relied on to hold the whole workflow."
    ]
  },
  "posture": ["execute", "provision", "admin"],
  "maturity": "ga",
  "applies_to": ["azure-sql-db"],
  "related": [
    "sql-database-projects",
    "schema-migrations-safely",
    "deploy-app-to-azure",
    "entra-id-auth",
    "provision-azure-sql-db",
    "connect-to-azure-sql",
    "azuresql-db-ci"
  ],
  "validation": {
    "target": "both",
    "setup": [
      "a repository that can run workflows, with a workflow able to request an identity token",
      "an application identity or user-assigned identity in the directory that can hold a federated credential",
      "a logical server and database already provisioned, since the workflow deploys schema and does not create infrastructure",
      "an identity with a role on the logical server resource if the deployment step is expected to manage a firewall rule"
    ],
    "assert": [
      "the federated credential subject was taken from what the platform presents rather than composed from a documented template, and the way it was obtained is stated",
      "the subject matches the job shape that will actually run, including the environment segment whenever the job names an environment",
      "the workflow grants the identity-token permission at the job or workflow level, because without it the login step has no token to exchange",
      "no database password, client secret or connection secret is stored in the repository, and the connection string carries an identity-based authentication keyword instead",
      "the plan states how the runner reaches the server through the firewall, and the chosen answer is named rather than left to the action's default behaviour",
      "an authentication failure is not reported to the user as a firewall failure, and the underlying message is read before the surface one",
      "the deployment step is given a target file whose type matches what the step is asked to do, and the action input naming the operation is supplied wherever it is required",
      "the runner has the deployment tool available, because the hosted image does not carry it",
      "the environment gate is stated to hold only the jobs that name it, and any job outside it that can reach the same secrets is called out"
    ],
    "probes": [
      {
        "where": "host",
        "cmd": "sqlpackage /Action:Publish /help:True | grep -q '^/TargetConnectionString:<string>$' && sqlpackage /Action:Publish /help:True | grep -q '^/SourceFile:<string>$'",
        "expect": { "succeeds": true },
        "why": "azure/sql-action does not have its own deployment engine: for Publish, Script and DeployReport it composes exactly '/Action:<action> /TargetConnectionString:\"<connection string>\" /SourceFile:\"<path>\"' and shells out, read from src/AzureSqlAction.ts at tag v2.4. The skill tells a reader to run that same line locally before trusting the pipeline, so both parameters have to be real. Measured 2026-09-03 on SqlPackage 170.4.83.3, both present in the Publish help. If a build renames either one, the local reproduction in the body stops working and so does the action."
      },
      {
        "where": "host",
        "cmd": "sqlpackage /Action:DeployReport /help:True | grep -q '^/OutputPath:<string>$'",
        "expect": { "succeeds": true },
        "why": "The check-it-worked section reruns DeployReport against the deployed database and counts operations in the XML, and the file only exists if the path parameter is right. Measured 2026-09-03 on 170.4.83.3: DeployReport takes /OutputPath. Microsoft Learn's SQL projects automation page uses the same parameter for this action."
      },
      {
        "where": "host",
        "cmd": "sqlpackage /Action:DeployReport /help:True | grep -q '^/DeployReportPath' && exit 1 || exit 0",
        "expect": { "succeeds": true },
        "why": "The negative half of the pair above, and the trap the skill names: /DeployReportPath is a Publish parameter and DeployReport does not accept it, so a report requested with the wrong flag is simply not written and the check silently measures nothing. Measured 2026-09-03 on 170.4.83.3: absent from the DeployReport help, present in the Publish help. This probe goes red the day the two actions converge, which would make the skill's warning wrong."
      },
      {
        "where": "host",
        "cmd": "sqlcmd '-?' | grep -q -- '--authentication-method'",
        "expect": { "succeeds": true },
        "why": "Before deploying anything the action opens a sqlcmd connection to master purely to read the runner's address out of the failure, and for an identity-based connection string it builds that call with --authentication-method=ActiveDirectoryDefault, read from src/SqlUtils.ts at v2.4. The skill prints that command line for a reader to run locally. Measured 2026-09-03 against sqlcmd 1.10.0 on this host; the action ships go-sqlcmd 1.6.0 into the runner tool cache, so a flag that disappeared between those versions would break the reproduction rather than the pipeline."
      },
      {
        "where": "host",
        "engine": true,
        "cmd": "set -eu\nd=$(mktemp -d); trap 'rm -rf \"$d\"' EXIT\nmkdir -p \"$d/db\"\ncat > \"$d/db/ShopDb.sqlproj\" <<'PROJ'\n<Project Sdk=\"Microsoft.Build.Sql/0.1.19-preview\">\n  <PropertyGroup>\n    <Name>ShopDb</Name>\n    <DSP>Microsoft.Data.Tools.Schema.Sql.SqlAzureV12DatabaseSchemaProvider</DSP>\n  </PropertyGroup>\n</Project>\nPROJ\ncat > \"$d/db/Product.sql\" <<'TBL'\nCREATE TABLE [dbo].[Product] ([ProductId] INT NOT NULL PRIMARY KEY, [Name] NVARCHAR(200) NOT NULL, [ListPrice] DECIMAL(10,2) NOT NULL);\nTBL\ndotnet build \"$d/db/ShopDb.sqlproj\" -p:NetCoreBuild=true >/dev/null\ntest -f \"$d/db/bin/Debug/ShopDb.dacpac\"\nsqlpackage /Action:Publish /TargetConnectionString:\"Server=tcp:localhost,${PROBE_PORT};Initial Catalog=${PROBE_DB};User ID=${PROBE_USER};Password=${PROBE_PASSWORD};Encrypt=True;TrustServerCertificate=True;\" /SourceFile:\"$d/db/bin/Debug/ShopDb.dacpac\" >/dev/null\nn=$(sqlcmd -S localhost,$PROBE_PORT -U \"$PROBE_USER\" -P \"$PROBE_PASSWORD\" -d \"$PROBE_DB\" -C -b -h -1 -W -Q \"SET NOCOUNT ON; SELECT COUNT(*) FROM sys.tables WHERE name = 'Product';\")\ntest \"$n\" = \"1\"",
        "expect": {
          "succeeds": true
        },
        "why": "THE FIRST PROBE ON THIS SKILL THAT DEPLOYS ANYTHING. The four before it read help output: they prove the parameter names in the skill's command lines are real and prove nothing about whether the pipeline works. This runs the action's three steps in order against a live engine, using the exact argument list the skill prints: dotnet build the .sqlproj, then sqlpackage /Action:Publish /TargetConnectionString: /SourceFile:, then a query for the table. It also pins the output path the action GUESSES rather than asks for, bin/<configuration>/<project>.dacpac, which the skill calls out as a trap. Red if the SDK moves that path, if the publish is refused, or if the table is not in sys.tables afterwards. A green run here is the claim 'this pipeline deploys schema' with something behind it."
      },
      {
        "where": "host",
        "engine": true,
        "cmd": "set -eu\nd=$(mktemp -d); trap 'rm -rf \"$d\"' EXIT\nmkdir -p \"$d/db\"\ncat > \"$d/db/ShopDb.sqlproj\" <<'PROJ'\n<Project Sdk=\"Microsoft.Build.Sql/0.1.19-preview\">\n  <PropertyGroup>\n    <Name>ShopDb</Name>\n    <DSP>Microsoft.Data.Tools.Schema.Sql.SqlAzureV12DatabaseSchemaProvider</DSP>\n  </PropertyGroup>\n</Project>\nPROJ\ncat > \"$d/db/Product.sql\" <<'TBL'\nCREATE TABLE [dbo].[Product] ([ProductId] INT NOT NULL PRIMARY KEY, [Name] NVARCHAR(200) NOT NULL, [ListPrice] DECIMAL(10,2) NOT NULL);\nTBL\ndotnet build \"$d/db/ShopDb.sqlproj\" -p:NetCoreBuild=true >/dev/null\nsqlpackage /Action:DeployReport /SourceFile:\"$d/db/bin/Debug/ShopDb.dacpac\" /TargetConnectionString:\"Server=tcp:localhost,${PROBE_PORT};Initial Catalog=${PROBE_DB};User ID=${PROBE_USER};Password=${PROBE_PASSWORD};Encrypt=True;TrustServerCertificate=True;\" /OutputPath:\"$d/after.xml\" >/dev/null\ntest -f \"$d/after.xml\"\ntest \"$(grep -c '<Operation ' \"$d/after.xml\" || true)\" = \"0\"",
        "expect": {
          "succeeds": true
        },
        "why": "The skill's own Check it worked step, which had never been run. It says to rerun DeployReport against the deployed database and expect grep -c '<Operation ' to be 0, because a publish that landed leaves the next report nothing to do. This asserts exactly that, against the database the previous probe published to. It also guards the parameter trap in the same section: /OutputPath is DeployReport's, while /DeployReportPath belongs to Publish, so the wrong flag writes no file at all. The test -f is there for that reason: without it a missing report would make grep -c print 0 and the probe would pass while measuring nothing, which is the exact green-that-means-nothing this lane exists to stop. Red if the publish left work behind, or if the report is not written."
      },
      {
        "where": "host",
        "engine": true,
        "cmd": "set -eu\nd=$(mktemp -d); trap 'rm -rf \"$d\"' EXIT\nmkdir -p \"$d/db\"\ncat > \"$d/db/ShopDb.sqlproj\" <<'PROJ'\n<Project Sdk=\"Microsoft.Build.Sql/0.1.19-preview\">\n  <PropertyGroup>\n    <Name>ShopDb</Name>\n    <DSP>Microsoft.Data.Tools.Schema.Sql.SqlAzureV12DatabaseSchemaProvider</DSP>\n  </PropertyGroup>\n</Project>\nPROJ\ncat > \"$d/db/Product.sql\" <<'TBL'\nCREATE TABLE [dbo].[Product] ([ProductId] INT NOT NULL PRIMARY KEY, [Name] NVARCHAR(200) NOT NULL);\nTBL\ndotnet build \"$d/db/ShopDb.sqlproj\" -p:NetCoreBuild=true >/dev/null\nsqlcmd -S localhost,$PROBE_PORT -U \"$PROBE_USER\" -P \"$PROBE_PASSWORD\" -d \"$PROBE_DB\" -C -b -Q \"INSERT INTO dbo.Product (ProductId, Name, ListPrice) VALUES (1, N'a', 9.99);\" >/dev/null\nif sqlpackage /Action:Publish /SourceFile:\"$d/db/bin/Debug/ShopDb.dacpac\" /TargetConnectionString:\"Server=tcp:localhost,${PROBE_PORT};Initial Catalog=${PROBE_DB};User ID=${PROBE_USER};Password=${PROBE_PASSWORD};Encrypt=True;TrustServerCertificate=True;\" /p:BlockOnPossibleDataLoss=true > \"$d/out.txt\" 2>&1; then\n  echo \"the publish SUCCEEDED and it was supposed to be blocked\"; cat \"$d/out.txt\"; exit 1\nfi\ngrep -qi \"data loss\" \"$d/out.txt\"\nn=$(sqlcmd -S localhost,$PROBE_PORT -U \"$PROBE_USER\" -P \"$PROBE_PASSWORD\" -d \"$PROBE_DB\" -C -b -h -1 -W -Q \"SET NOCOUNT ON; SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('dbo.Product') AND name = 'ListPrice';\")\ntest \"$n\" = \"1\"",
        "expect": {
          "succeeds": true
        },
        "why": "/p:BlockOnPossibleDataLoss=true is the one deployment property the skill names, and arguments: is the input that carries it through to the command line. Nothing had ever checked that it blocks. The probe publishes a dacpac whose Product table has lost the ListPrice column, against a database where that column holds a row, and asserts three things: the publish exits non-zero, its output says data loss, and the column is STILL THERE afterwards. The last assertion is the one that matters. A refusal that had already dropped the column would be worse than no refusal, and an exit code alone cannot tell the two apart. Red if the property stops blocking, or if it blocks after the fact."
      },
      {
        "sql": "BEGIN TRY\n  EXEC sp_executesql N'USE [master];';\n  SELECT -1 AS unreached;\nEND TRY\nBEGIN CATCH\n  SELECT ERROR_NUMBER() AS n;\nEND CATCH",
        "expect": {
          "rows": [
            [
              40508
            ]
          ]
        },
        "why": "Why the action opens TWO connections rather than one. The skill states the firewall probe runs 'against master first, the target database second', and the reason is that this engine refuses to switch database inside a session: USE raises Msg 40508. An agent that read that sentence as a stylistic choice would collapse the two calls into one connection with a USE and get a pipeline that fails after the login succeeds. The catch is narrow on purpose: it asserts the NUMBER, so a different failure, including a malformed batch, reports its own number and the probe goes red rather than passing on any error at all. Red if this engine ever allows USE."
      }
    ]
  },
  "triggering": {
    "explicit": [
      "Use the github-actions-for-sql skill",
      "write a GitHub Actions workflow that deploys my database project to Azure SQL Database",
      "set up federated credentials so my workflow logs in to Azure without a stored password",
      "gate my database deployment on an environment approval",
      "publish this dacpac from CI on merge to main"
    ],
    "implicit": [
      "my workflow logs in fine until I add an environment to the job, then it fails",
      "no matching federated identity record found for presented assertion subject",
      "the deploy step says it cannot detect the client IP address",
      "how does the runner get through the Azure SQL firewall when its address changes every run",
      "the pipeline cannot find sqlpackage on the runner",
      "I do not want a SQL password sitting in repository secrets"
    ],
    "routing": [
      "which action deploys a sqlproj rather than a prebuilt dacpac",
      "can the workflow produce a deployment script instead of deploying",
      "what permissions does the workflow need to request a token",
      "do environment approvals stop other jobs in the same workflow",
      "why does the login error mention auth-type"
    ],
    "negative": [
      "set the target platform on my SQL database project so the build validates against Azure SQL Database",
      "my dacpac publish reported success and the column data is gone",
      "should this column change be done as expand and contract",
      "deploy my web app and its database to Azure with azd",
      "create the database user for my application identity and grant it roles",
      "run integration tests against a local database container in CI",
      "create an Azure SQL database and a logical server in my subscription",
      "add retry logic to my data access layer"
    ]
  }
}
